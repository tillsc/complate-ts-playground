{
  "name": "complate-ts-demo",
  "description": "complate with TypeScript demo project",
  "version": "0.0.1",
  "license": "MIT",
  "devDependencies": {
    "complate-stream": "complate/complate-stream#typescript",
    "faucet-pipeline-js": "^0.15.7",
    "npm-run-all": "^4.1.2",
    "typescript": "^2.6.2"
  },
  "scripts": {
    "watch": "npm-run-all --parallel tsc-watch faucet-watch",
    "tsc-watch": "./node_modules/.bin/tsc --watch",
    "faucet-watch": "./node_modules/.bin/faucet --no-fingerprint --watch",
    "compile": "./node_modules/.bin/tsc && ./node_modules/.bin/faucet --no-fingerprint"
  }
}
